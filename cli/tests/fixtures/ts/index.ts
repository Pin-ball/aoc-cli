import { lines } from "./helper.ts";

export const part1 = (input: string) => {
  console.log("printed, not answered");
  return lines(input).length;
};

export const part2 = (_input: string) => {
  throw new Error("not written yet");
};
